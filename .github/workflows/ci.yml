name: CI

on:
  # Run on every pull request...
  pull_request:
  # ...and on pushes to main, so the branch's status is always current.
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      # Source hygiene is asserted by unit tests; this asserts the ARTIFACT.
      # A VITE_-prefixed secret is inlined at build time and would appear only
      # here, so the check has to run against dist/ after the build.
      - name: Verify no secrets in the client bundle
        run: npm run verify:bundle
